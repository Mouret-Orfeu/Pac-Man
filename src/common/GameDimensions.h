#pragma once

// Tile size
static constexpr int TILE_SIZE = 8;

// Dimensions
static constexpr int WINDOW_COLS = 28,
                     WINDOW_ROWS = 36;
static constexpr int MAZE_COLS = 28,
                     MAZE_ROWS = 31;
static constexpr int WINDOW_WIDTH  = WINDOW_COLS*TILE_SIZE,
                     WINDOW_HEIGHT = WINDOW_ROWS*TILE_SIZE;

// Sprite size
static constexpr int SPRITE_SIZE = 16;
